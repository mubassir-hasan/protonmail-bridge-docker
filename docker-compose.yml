version: '3.9'
services:
  protonmail-bridge:
    build: 
      context: ./deb
      dockerfile: Dockerfile
    container_name: protonmail-bridge
    volumes:
      - protonmail:/root
    ports:
      - "127.0.0.1:1028:1025"
      - "127.0.0.1:1165:1143"
    restart: unless-stopped

networks: 
  default: 
    name: protonmail_bridge
volumes:
  protonmail: